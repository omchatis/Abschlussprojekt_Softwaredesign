def build_symetry_axis(st, nodes):
    xs = [node.x for node in st.nodes.values()]
    x_sym = 0.5 * (min(xs) + max(xs))
    return x_sym

def key(x, z):
        return (c,z)

def check_symetry(st, nodes): 
    for nid in nodes:
        (node.bc[0] == True) or (node.bc[1] == True)
        return nid.x and nid.y


def check_symetry():

